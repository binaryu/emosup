# Changelog

本文件的每个版本段落会被自动写入对应的 GitHub Release 更新说明。

## v1.3.1

### 新增
- **BT 下载（qBittorrent 集成）**：新「BT 下载」页支持直接添加磁力链接，实时显示种子进度/做种状态/速度/做种比例，下载完成后一键扫描（输 TMDB ID）转存；可查看文件列表、暂停/继续、删除种子；「系统配置 → qBittorrent」填写 WebUI 地址/账密/保存目录，带测试连接；BT 任务上传后强制保留本地文件，做种不中断
- **上传后保留本地文件**：任务与扫描项均可标记保留（BT/PT 做种或本地留档），上传完成后不再自动删除本地文件；「系统配置 → 本地媒体」可设全局默认，BT 来源任务强制保留
- **缓存管理**：新「缓存管理」页可视化下载缓存目录——标记孤儿文件/临时分片/任务引用文件（含任务状态与保留标记），支持勾选批量删除、「全选未引用」一键清理；自动跳过 BT 做种目录；使用中的任务文件拒绝删除

### 技术
- `keep_local_file` 落库（SQLite 新列 + 存量库自动迁移），新建/批量插入/读取全链路打通
- qBittorrent 客户端基于 WebUI v2 API（登录/加种/列表/文件/暂停/继续/删除），BT 扫描复用本地源扫描流程（路径校验限制在本地媒体根目录内）

## v1.3.0

### 上传卡死修复
- 上传分片新增卡死检测：连接 60 秒无数据流动立即报错（原实现静默等待 10 分钟 × 重试 5 次，最长 50 分钟无反馈），慢速但正常的上传不受影响
- 上传传输层快速失败：TCP 拨号 15s / TLS 握手 15s / 响应头 60s 超时，国内 VPS 连不上 R2 等存储时秒级报错并显示具体原因；单次请求总超时 10 分钟 → 3 分钟

### 下载可靠性
- 多线程下载分段隔离重试：单个分段失败只重试该分段（最多 3 次，指数退避），不再整体回退单线程重新下载；新增回归测试
- 下载客户端增加响应头超时 30s，上游挂死不再无限等待

## v1.2.9

### 新增
- 扫描结果批量删除：「删除选中」一次请求删除所有勾选扫描项（新增批量接口 `DELETE /api/scans/:id/items`，单事务完成）；「删除扫描」仍删除整个扫描
- 批量多选交互（表格视图）：按住 Ctrl 滚动滚轮勾选光标经过的行、Ctrl+点击切换单行、Shift+点击区间多选
- 删除操作独立 loading 状态，不再连带「刷新」按钮闪动

## v1.2.8

### 新增
- 自定义网页标题：「系统配置 → 基础服务 → 网页标题」可自定义浏览器标签页与侧边栏顶部显示的名称，留空恢复默认
- 扫描性能优化：扫描视频文件并发处理（12 并发获取直链），530 集长番扫描从串行 100+ 秒降到约 10 秒，不再触发浏览器 90 秒超时；Emos 视频树预索引，匹配从 O(n²) 降为 O(1)
- 集数识别新增「开头数字 = 集数」规则（如 `001 狼来了(上).mp4`、`530 神秘大三角 6.mp4`），支持 500+ 集长番；修复文件名末尾数字被误判为集数的问题

## v1.2.7

### 修复
- 修复面板升级"点击没反应"：升级改为后台异步执行，不再受浏览器 90 秒超时中断；下载/安装过程可通过新接口 `/api/upgrade/status` 实时查看阶段与错误，失败时面板直接显示原因（如网络/代理下载失败的具体报错）
- 升级下载日志记录实际 URL（含代理前缀），可在「运行日志 → 服务日志」确认是否走了 `EMOSUP_PROXY` 代理

## v1.2.6

### 修复
- 真正修复 multipart/R2 分片上传速度虚高（实测 400MB/s 而实际带宽仅 10MB/s）：
  - 根因：Go HTTP 传输层会把请求体预读到内存，基于 body 读取的进度回调反映的是**本地磁盘读取速度**而非网络速度，速度被虚抬几十倍
  - 修复：上传速度与进度改为只按「分片完成（服务端确认）」在墙钟窗口内采样，1 秒节流推送；分片传输期间速度保持/缓慢衰减而非虚高
  - 新增真实回归测试：10MB/s 节流服务器 + 真实 HTTP 客户端上传，断言上报速度不超过上限（修复前该测试测得 4600MB/s）

## v1.2.5

### 修复
- 分片重试重复上报字节导致的进度/速度虚增（v1.2.6 修复了更主要的预读虚高问题）

## v1.2.4

### 自动调优（默认开启，可关闭）
- 根据实测带宽（AIMD 拥塞控制式）与剩余磁盘空间，自动提高并行任务数、下载线程、上传分片与分片并发
- 只在手动配置值之上浮动，永不低于手动设置；可在「系统配置 → Worker → 自动调优」关闭

### 升级可靠性修复
- 修复面板内升级后服务不自动恢复的问题：升级完成后以非零码退出，由 systemd `Restart=on-failure` 自动拉起，不再依赖易被 systemd 清理的重启脚本
- 重启脚本按真实 unit 名（从 cgroup 解析）执行 systemctl restart → start → 直接 re-exec 逐级兜底
- 升级全程日志写入 `/tmp/emosup-upgrade.log`

### 面板内日志查看
- 「系统配置 → 运行日志」直接查看服务日志（journalctl）与升级日志，无需 SSH

## v1.2.3

### 性能与实时性
- 下载/上传速度改为瞬时速度 + EMA 平滑，不再显示全程平均；新增 ETA（剩余时间）显示
- 上传分片内实时上报进度与速度，慢速大文件不再长时间无刷新
- 修复 SSE 进度事件触发全量刷新导致的页面卡死（请求风暴）

### aria2 移除与配置迁移
- 删除已无实际用途的 aria2 RPC 代码路径，下载统一走内建下载器
- 配置 `aria2.download_dir` 自动迁移为 `download.dir`，旧数据库自动清理 `dl_aria2_gid` 列

### 其他
- 修复取消下载不生效的问题（取消后任务可能继续下载并被覆盖状态）
- 修复本地媒体浏览根目录为 `/` 时子目录全部 access denied 的问题
- 新增本地媒体/扫描结果页面的移动端卡片视图
- 面板内一键升级功能（检查/下载/校验/替换/自动重启）
- 界面显示当前版本号
