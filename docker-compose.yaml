services:
  emosup:
    image: ghcr.io/binaryu/emosup:beta-20260323-214646
    container_name: emosup
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./backend/data:/app/backend/data
